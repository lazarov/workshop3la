%h1 Users

.col-lg-12
  %table.table.table-striped.table-hover
    %tr
    %th
      %th
      %th Name
      %th Email
      %th

    - users.each_with_index do |user, i|
      %tr
        %td
        %td= (i + 1)
        %td= user.firstname
        %td= user.email
        %td= link_to 'Show', user_path(user.id), class: 'btn btn-sm btn-primary'
